# Module 02 — QA evidence (2026-09-07/08)

Environment: `npm run dev` started through `.claude/launch.json` → `dev-local` (`JUDGE_BACKEND=local`: javac 17 / python3 / Apple clang g++ / node 22 on the host),
Firestore project `algobook-c9caa` (wiped by the owner at 17:49 UTC and re-seeded during this module), OpenAI `gpt-5.6-luna` / `gpt-5.6-terra`,
desktop Browser pane (dark theme, ~800×455 viewport), signed in with custom tokens as `biu416…` (admin, free plan) and `Wa9Ms…` (pro).

Why the local judge: the Judge0 RapidAPI free tier allows 50 batched submissions per day (D-05) and it was at 0 when this module started.
Every problem below was still verified by executing its reference solution against every sample + hidden test; the executor was the host toolchain
instead of Judge0. Two Sum was re-seeded the same way (`JUDGE_BACKEND=local npm run db:seed:sample`: 13/13 in Java, Python, C++, JavaScript).

## Automated
- `npm test` — 55 vitest tests (encoding linter, model prices/overrides, prompt snapshot + injection input, spec validation + reference-output adoption, client retries/refusal/schema-retry, sanitizer, topics): green.
- `npm run typecheck`, `npm run lint` (0 errors), `npm run build`: green. `grep -r "OPENAI\|RAPIDAPI_KEY" .next/static` → nothing. `grep -r "gpt-" src | grep -v models.ts` → nothing.
- `npm run api:smoke -- --uidA … --uidB …` — **59/59** after the chat check was moved before the user solves the problem (`api-smoke.log` is the 58/59 run; the one failure was the ordering, not the feature — the refusal on an unsolved problem was re-tested by hand, reply: "I can’t provide the full solution while it’s unsolved, but here’s the next step: …", no code).
  Module 02 checks in that run: free user → 402 on editorial / hint 3 / completion; pro `POST /api/projects/:id/next` → `reused` Two Sum in 1.5 s; private tests ≥ 8; stored reference → AC 13/13 via `/api/submit`; review score 9.5; free user → `reused` "Forward Difference Pair Count" in 1.4 s; contextual hint 3 mentions the code; editorial 2 approaches then cached in 403 ms; explain-error 51 words; completion ≤ 6 lines; chat SSE deltas + done; insights 3 milestones + weekly plan; admin `ai-usage` totals (58 calls).
- `npm run ai:eval` (no persistence, local judge). Generation on Luna `high`:
  | run | n | first pass (raw) | verified after repairs | mean cost | p50 | p95 | notes |
  |---|---|---|---|---|---|---|---|
  | run2 (`ai-eval-run2-high.log`) | 3 | 0/3 | 3/3 | $0.029 | 117 s | 192 s | before adoption/dedupe; failures: 1 wrong hidden expectation (WA 13/14), 2 duplicated sample inputs; one repair went to Terra ($0.047) |
  | run3 (`ai-eval-run3-high-adoption.log`) | 4 | 2/4 | 4/4 | $0.015 | 137 s | 149 s | both failures were "hidden test duplicates a sample" — now dropped in `validateSpec` instead of repaired, so all 4 would verify without a repair |
  | run4 (`ai-eval-run4-medium.log`, `AI_REASONING_OVERRIDE_GENERATE=medium`) | 4 | 2/4 | 3/4 | $0.010 | 37 s (verified ones) | 157 s | first-pass generations took **36–37 s at $0.004–0.005**; one Hard spec had a sample input in the wrong encoding (repaired, 157 s); one Medium/stack output hit the 16k budget with only 1.9k reasoning tokens — the model wrote enormous test inputs |
  | run5 (`ai-eval-run5-medium-compact.log`, medium + "compact tests" rule) | 2 | 0/2 raw, 1/2 verified without repair | 1/2 | — | 29 s | — | matrix: one wrong hidden expectation **adopted from the reference**, verified in 29 s for $0.0044; stack: truncated again (16k output, 2.4k reasoning) → led to the automatic COMPACT retry (see below) |
  | run6 (`ai-eval-run6-stack-compact-retry.log`, medium, stack only, with the automatic COMPACT retry) | 2 | 2/2 after retry | 2/2 | $0.003 (+ $0.019 per discarded truncated call, not included) | 110 s | — | both first attempts degenerated into a repeating pattern (`<a<a<a…`, `()()()…`) while writing a long string stress test and hit the 16k budget; the retry with the COMPACT directive verified first pass both times |
  Root causes seen so far, in order (n = 19 generations across runs 2–6): hidden test repeats a sample input (3×, now auto-dropped), one hand-computed hidden expectation wrong while all samples pass (2×, now auto-adopted from the reference), output truncated — first at the planned 6k `max_output_tokens` because reasoning tokens count (fixed: 16k/20k budgets), then 4× at 16k because the model wrote degenerate repeated string tests (now retried once with a COMPACT directive, 2/2 rescued), one sample input in the wrong stdin encoding (1×, repaired). Repair ladder success: 5/5.
  Reasoning effort: `high` (the D-03 default, kept) costs $0.006–0.012 and 55–80 s per generate call; `medium` measured $0.004–0.005 and 29–37 s with the same verification outcome on this small sample — a `AI_REASONING_OVERRIDE_GENERATE=medium` switch is the owner's call (D-03). The ≥ 85 % first-pass target could not be measured on a large sample within this session (each generation is 1–2 minutes); across runs 3–6 with the current fixes, 8 of 10 completed generations verified without any repair call.

## Browser checklist (module §6) — `/dev/api-smoke`, Module 02 section
| # | Check | Result |
|---|---|---|
| 1 | Generate next with prompt "medium sliding window" → stages stream → problem appears; starter loads; reference → AC | ✓ stages `1.6s searching · 3.1s generating · 63.4s validating · 64.0s verifying r0 · 64.5s repairing r1 gpt-5.6-luna · 80.8s verifying r1 · 80.9s persisting · 81.0s done (generated)`; "Integer-Mean Windows · generated · attempts 2 · 79 s · $0.0092"; starter loaded; reference solution (pasted from `scripts/print-reference.ts`) → **Accepted 14/14** |
| 1b | Background fan-out | ✓ python ready 5 s ($0.0008), javascript 17 s ($0.0024), cpp failed on the first run (Apple clang has no `bits/stdc++.h`) → local shim added → "cpp: ensure" showed **Preparing…** then ready (5.8 s, $0.0005); `referenceRuntimeMs` recorded per language |
| 2 | Same prompt again → `reused`, near-instant | ✓ via api-smoke: pro user → `reused` "Two Sum" 1.5 s; free user → `reused` "Forward Difference Pair Count" 1.4 s (the in-browser repeat generated instead because Two Sum had been wiped minutes earlier and prompt-topic parsing was still naive — both fixed: Two Sum re-seeded, `topicsInText`/`difficultyInText`) |
| 3 | Hints 1/2/3; hint 3 as free → 402 | ✓ free: hint 1 stored ($0), hint 3 → `402 PAYMENT_REQUIRED: "hint3" is not included in the free plan`; pro: hint 3 contextual ($0.0002) — "Your loop condition skips the final valid window; iterate while the window’s end index remains within the array. Also use a long for the window sum…" (the pasted code had `i + k < nums.length` and int overflow) |
| 4 | Editorial renders; second open instant | ✓ pro: generated in 9.8 s (overview + 2 approaches with Java code + pitfalls), second call **cached 447 ms**; free → 402 |
| 5 | Wrong answer → Explain error on a compile error; chat "why does my loop fail?" streams; refuses "write the full solution" | ✓ CE submit → explanation in 4 s (names the `String`→`int` assignment and the missing method); chat streamed a Socratic reply; "Just write the full solution" on a solved problem gave a code fragment (allowed when SOLVED=true) — on an unsolved problem (re-test by curl) it declined and gave the next step only, after the tutor rule was tightened |
| 6 | Inline completion | ✓ prefix ending in `for (int i = 0;` → 6-line ghost text in 2.6 s; free user → 402 (the 600 ms debounce / setting toggle is Module 03 UI) |
| 7 | Admin AI-usage | ✓ `GET /api/admin/ai-usage` → totals by purpose/model/day (e.g. 30 calls, $0.128, generate 7 / repair 5 / driver 6 …) |

Not captured: page screenshots at 1440×900 / 390×844 in light and dark. The only UI in this module is the temporary admin smoke page that Modules 03/05 replace, and it requires a custom-token session that headless Chrome cannot reproduce; the raw API responses in `api-smoke.log` and the log excerpts above are the evidence.
