# Decisions & Open Questions

Each decision has a **default** that modules must follow while the decision is `OPEN`. When the owner (Avinash) answers, change the status to `DECIDED`, record the answer, and update any module that referenced it.

| ID | Question | Default (used until decided) | Status | Answer |
|---|---|---|---|---|
| D-01 | **Which programming languages should v2 support?** Every language needs its own verified driver per problem (extra AI + judge cost per problem, ~$0.003 each on Luna). | ~~Java + Python~~ | **DECIDED 2026-09-07** | **Java, Python 3, C++, JavaScript — all four enabled at launch.** Java driver is produced at generation time; the other three are generated + verified lazily on first request per problem **and** eagerly by the pre-generation job for pool/template problems, so pool problems arrive with all four verified. Language picker shows all four; a not-yet-verified language shows "Preparing…" for ~10 s. |
| D-02 | **Wipe Firebase?** You said history can be deleted. Full wipe means all users lose projects, submissions, streaks and usernames. | ~~Keep `subscriptions`~~ | **DECIDED 2026-09-07** | **Wipe everything, including `subscriptions`** (JSON backup to `backups/<timestamp>/` first). Paid users, if any, are re-granted manually via the admin console or a `scripts/grant-plan.ts` helper (Module 01 adds it). |
| D-03 | **Model tiers.** Luna for everything with Terra fallback is ~10× cheaper than today. Sol/Astra give better first-pass correctness on Hard problems but cost 20–50×. | Luna `high` → Luna `xhigh` (with judge feedback) → Terra `medium`. Sol/Astra reserved for an admin-only "premium regenerate" button. | OPEN | |
| D-04 | **Free tier.** Today free users can do nothing except look at the dashboard, which limits growth. | Free: 3 AI generations/day, unlimited curated-pool problems, 30 runs/day, hints level 1–2, no editorial/chat/completion. Pro: unlimited (soft cap 200 generations/day for abuse). Quotas in `users.quotas`, enforced server-side. | **DECIDED 2026-09-07** | Default confirmed. |
| D-05 | **Code execution backend.** Judge0 CE on RapidAPI has strict per-day quotas on lower plans and adds latency. Self-hosting Judge0 (Docker on a small VPS, ~$10/mo) removes the quota and supports batch natively. | Keep RapidAPI for now behind a `JUDGE0_BASE_URL`/`JUDGE0_AUTH_HEADER` abstraction so switching to self-hosted is a config change. | **DECIDED 2026-09-07** | Default confirmed. Module 01 ships `docs/modules/reference/JUDGE0-SELF-HOST.md` (docker-compose + env switch) for later. |
| D-06 | **Public problem bank ("Explore")** listing every verified problem for all users (LeetCode-style problemset page with filters). | Yes — verified `problems` are shared; Explore page in Module 05; solving from Explore creates/uses a personal "Explore" project. | OPEN | |
| D-07 | **Attendance modal.** Keep the "Mark your attendance" modal? | Remove. Daily check-in becomes automatic on first Run/Submit of the day (server-side `activity` doc); streak UI shows in the top bar. | OPEN | |
| D-08 | **Branding.** Keep the name "AlgoBook", logo mark, and indigo accent? | Keep name; new logo mark (Module 05); brand accent indigo→cyan gradient; workspace uses LeetCode-like neutral dark. | OPEN | |
| D-09 | **3D.** Where should 3D (React Three Fiber) appear? | Landing hero + login left panel only. App pages use 2D motion (framer-motion) for performance. Fallback static image when WebGL unavailable. | OPEN | |
| D-10 | **Editorials.** AI-generated editorial per problem, unlocked when? | Generated once per problem (shared). Unlocked after the user's first Submit (any verdict) or by explicitly clicking "Reveal editorial" (records `editorialViewed`, lowers mastery gain for that solve). Pro-only in D-04 default. | OPEN | |
| D-11 | **Mock interview mode** (timed 45-min session, 2 problems, AI interviewer feedback) — include in v2? | Include in Module 04 as a stretch task; ship only if 04 core tasks are complete. | OPEN | |
| D-12 | **Admin console** (AI cost, flagged problems, pre-generation trigger) gated by `ADMIN_UIDS` env. | Include (Module 05, minimal). | OPEN | |
| D-13 | **Payments.** Keep the CQ gateway with manual renewal (no auto-renew)? | Keep as is; add `NEXT_PUBLIC_APP_URL` to env; add webhook-less activation hardening (Module 01). | OPEN | |
| D-14 | **Hosting.** Vercel? (matters for cron jobs and function timeouts — generation+verification can take 30–90 s). | Assume Vercel Pro (route handler `maxDuration = 300`). Pre-generation uses OpenAI Batch API + a cron route. If not Vercel, tell the assistant which platform. | OPEN | |
| D-15 | **Community features** (public solutions, comments, likes) like LeetCode's Solutions tab. | Not in v2. Solutions tab shows AI reference solutions per language + the user's own accepted submissions. | OPEN | |
