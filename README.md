# AlgoBook AI (v2)

AI-verified, LeetCode-style interview practice. Problems are generated by AlgoBook AI
(`gpt-5.6-luna`), **executed against every hidden test on the judge before anyone sees
them**, and solved in a LeetCode-parity Monaco workspace with server-side Run/Submit,
an AI tutor, per-user practice intelligence (mastery, spaced repetition, rating,
streaks, achievements), a daily challenge, leaderboards, mock interviews and a Pro plan.

> v2 shipped in five modules (2026-09-07 → 2026-09-08). The plans, status log and
> decisions live in [`docs/modules/`](docs/modules/00-MASTER-PLAN.md) — read
> `00-MASTER-PLAN.md` first, then `STATUS.md`.

## Product

| Page | What it does |
|---|---|
| `/` | Marketing landing: 3D hero (R3F), **live in-browser demo workspace** (edit + run JavaScript against sample tests, no sign-in), feature bento with real screenshots, how it works, comparison vs LeetCode, pricing (Free / Pro ₹499 / ₹4,999, manual renewal), FAQ, JSON-LD |
| `/login` | Google sign-in with `?next=` redirect and popup-blocked fallback |
| `/dashboard` | Continue · Daily challenge · Streak & XP · Rating cards, projects, activity heatmap, recommended next (Module 04 recommender), 3-step onboarding for new users |
| `/explore` | Public problem bank (D-06): filters (difficulty, status, topics, company, rating), sort, virtualised table, "Pick one for me"; filters live in the URL |
| `/projects/new` · `/projects` · `/project/:id` | 4-step wizard (template or custom → goal & level → details & pace → review); overview with Plan (AI insights), Problems, Activity, Settings tabs |
| `/problems/:slug` · `/project/:id/solve/:problemId` | The workspace (Module 03): resizable panels, testcases, Beats %, editorial, solutions, submissions, notes, timer, tutor chat, Vim/Emacs |
| `/daily` · `/interview` · `/leaderboard` | Daily challenge + 30-day calendar; mock interview (start, resume, AI debrief); global / weekly / company-cohort boards with podium and my-rank |
| `/profile` · `/:username` · `/profile/edit` | LeetCode-density profile: solved ring with Beats %, badges, skills by level, skill radar, rating history, heatmap, recent AC; public page hides private data; per-user OG image |
| `/settings` | Account, plan & billing (checkout, invoices, limits), appearance, editor defaults (synced with the workspace), notifications, privacy (public profile), delete account |
| `/admin` | AI spend (by day / purpose / model), pool coverage heat table + pre-generation trigger, flagged-problem queue (retire / restore), leaderboard & daily triggers (`ADMIN_UIDS`) |

Screenshots (desktop 1440×900 + mobile 390×844, dark + light): `docs/modules/qa/05/`.
Design system: tokens in `src/app/globals.css`, primitives in `src/components/ui`, motion kit in
`src/components/design/motion.tsx`, charts in `src/components/charts`, 3D hero in `src/components/design/HeroScene.tsx`.

## Stack

Next.js 16 (App Router, Turbopack, TypeScript 7) · React 19 · Tailwind v4 + shadcn/ui ·
Firebase Auth + Firestore (client read-only, all writes via firebase-admin) ·
Judge0 CE (RapidAPI, self-host ready) · OpenAI (gpt-5.6-luna) · zod · vitest.

## Setup

```bash
npm install
cp .env.example .env.local        # fill in the values (see comments in the file)
npm run dev                       # http://localhost:3000
```

Required services:

1. **Firebase** — a project with Google sign-in enabled; put the service-account
   JSON under `firebase/` (git-ignored) and point `FIREBASE_SERVICE_ACCOUNT_PATH` at it.
   Deploy rules and indexes once: `npm run db:deploy` (or `firebase deploy --only firestore:rules,firestore:indexes`).
   Until they are deployed, list endpoints fall back to in-memory ordering and log `firestore.missing_index`.
2. **Judge0** — a RapidAPI key for `judge0-ce.p.rapidapi.com`, or a self-hosted
   instance (see `docs/modules/reference/JUDGE0-SELF-HOST.md`).
   **The RapidAPI free tier allows 50 batched submissions per day**, and Run and Submit
   use exactly one batch each, so the whole deployment supports ~50 code executions
   per day. `/api/run` and `/api/submit` return `502 UPSTREAM "Judge0 rate limit
   reached"` once it is spent (`x-ratelimit-batched-submissions-remaining: 0`, resets
   on a rolling ~24 h window). Self-hosting removes the cap and is a config change.
3. **OpenAI** — `OPENAI_API_KEY` (Module 02).
4. **ADMIN_UIDS** — comma-separated Firebase UIDs allowed to use admin routes and
   `/dev/api-smoke`. Find yours: `npm run db:find-uid -- --email you@example.com`.

## Scripts

| Command | What it does |
|---|---|
| `npm run dev` / `build` / `start` | Next.js |
| `npm run typecheck` | `tsc --noEmit` (TypeScript 7) |
| `npm run lint` | ESLint 10 flat config (`eslint-config-next`) |
| `npm test` | vitest unit tests (checkers, quotas, stdin encoding, assembler, template parser) |
| `npm run db:status` | Prints collection counts and whether the seeded fixtures are present |
| `npm run db:deploy` | Publishes `firebase/firestore.rules` + `firestore.indexes.json` with the service account (same as `firebase deploy --only firestore:rules,firestore:indexes`, no CLI login needed) |
| `npm run db:seed:templates` | Seeds `templates/{company}` from `templates/*.md` |
| `npm run db:seed:sample` | Seeds the verified "Two Sum" problem (Java, Python, C++, JavaScript) after running each reference solution on Judge0 |
| `npm run db:grant-plan -- --uid <uid> --plan pro-yearly --days 365` | Grants a Pro plan manually |
| `npm run db:wipe -- --yes` | Backs up every collection to `backups/<timestamp>/` then deletes everything (`--dry-run` = backup only) |
| `npm run dev:token -- --uid <uid>` | Mints a custom token + ID token for local testing without the Google popup |
| `npm run api:smoke -- --uidA <uid> --uidB <uid>` | Headless API acceptance suite (Modules 01/02/04) |
| `npm run qa:screenshots -- --uid <uid> --urls /dashboard,/explore --out docs/modules/qa/05 [--axe] [--noauth] [--format webp]` | Headless Chrome captures at 1440×900 / 390×844 in dark + light, optional axe-core audit |
| `npm run qa:user -- --create` / `--delete --uid <uid>` | Throwaway Auth account for the fresh-account walkthrough |
| `npm run db:migrate:stats`, `npm run load:leaderboard` | Module 04: stats migration and the leaderboard load test |

## Architecture (Module 01 — Foundation)

```
src/
  app/api/**            route handlers: every call authenticated with a Firebase ID token
  lib/env.ts            zod-validated environment (fails fast)
  lib/api/              ApiError envelope + handler() wrapper (zod body/query, auth, quota, structured logs)
  lib/auth/             requireUser (verifyIdToken + plan cache + quotas), PLAN_LIMITS
  lib/data/             schema.ts (zod schema v2 = source of truth) + typed Firestore repositories
  lib/judge/            Judge0 batch client, per-language assembler, canonical stdin encoding, checkers, service
  lib/practice/         practice-intelligence engine (Module 04): mastery, SRS, rating, recommender, streaks, achievements, interview
  lib/app/              browser data layer for the pages (Module 05): useQuery cache, typed API client, formatters
  components/ui|design|charts|shell|marketing|landing|dashboard|explore|profile|project|wizard|admin  (Module 05)
  components/workspace  the LeetCode-parity problem page (Module 03)
  lib/api-client.ts     apiFetch() for the browser — attaches the ID token, throws ApiError
  types/index.ts        types derived from the zod schemas
scripts/                firestore-wipe, seed-templates, seed-sample-problem, grant-plan, find-uid, dev-token, api-smoke, qa-screenshots, qa-user
firebase/               firestore.rules (v2, deny-by-default) + firestore.indexes.json (incl. vector index)
docs/modules/           master plan, per-module specs, STATUS.md, DECISIONS.md, reference docs
```

### API (Module 01)

All routes require `Authorization: Bearer <Firebase ID token>` and return errors as
`{ error: { code, message, details? } }` (`401 UNAUTHENTICATED`, `402 PAYMENT_REQUIRED`,
`429 QUOTA_EXCEEDED { resetAt }`, `404` for anything you do not own).

| Route | Purpose |
|---|---|
| `GET/PATCH /api/me`, `PATCH /api/me/settings`, `POST /api/me/username`, `GET /api/users/username/check?u=` | profile, settings, username |
| `GET/POST /api/projects`, `GET/DELETE /api/projects/:id` | projects with denormalised progress |
| `GET /api/problems`, `GET /api/problems/:id?lang=` | explore list; public problem + starter + sample tests (hidden tests never leave the server) |
| `POST /api/run` | up to 6 cases in **one** Judge0 batch |
| `POST /api/submit` | sample + hidden tests in one batch → verdict, beats %, submission, activity, project progress, user counters |
| `GET /api/submissions`, `GET /api/submissions/:id`, `PUT /api/drafts/:id`, `PUT /api/notes/:id`, `GET /api/activity` | history, autosave, heatmap |
| `POST /api/problems/:id/report`, `GET /api/templates`, `/api/subscription/*` | flagging (auto-retire at 2), templates, payments |

Problem I/O contract (stdin/stdout encodings shared by all languages):
`docs/modules/reference/IO-FORMAT.md`.

## Contributing to the rebuild

Work happens one module at a time on `module/0N-*` branches; each module file
has tasks, acceptance criteria and a browser test checklist. See
`docs/modules/00-MASTER-PLAN.md §1` and `§10` for the workflow and the
Definition of Done.

## AI engine (Module 02)

- `POST /api/projects/:id/next` recommends, reuses a verified problem or generates one (`gpt-5.6-luna`, structured outputs), verifies the reference solution on the judge, repairs on failure, and streams stages with `?stream=1`. Python, C++ and JavaScript drivers are generated and verified in the background.
- Hints, editorial, code review, explain-error, tutor chat (SSE), inline completion, project insights: `src/lib/ai/features.ts` and the routes under `src/app/api/problems/[id]/*`, `src/app/api/ai/complete`.
- Model ids and prices live only in `src/lib/ai/models.ts` (`AI_MODEL_OVERRIDE_<PURPOSE>` / `AI_REASONING_OVERRIDE_<PURPOSE>` for experiments). Every call is logged to `aiUsage`; `GET /api/admin/ai-usage` aggregates it.
- Pre-generation of the topic×difficulty pool uses the OpenAI Batch API: `POST /api/admin/pregen` (`submit` / `collect` / `status` / `deficits`) or the hourly `GET /api/cron/pregen` with `Authorization: Bearer $CRON_SECRET`.
- `npm run ai:eval -- --n 15` measures first-pass verification, repair success, cost and latency without persisting. Set `JUDGE_BACKEND=local` (host javac/python3/g++/node; development only) to avoid the Judge0 RapidAPI daily quota — the `dev-local` entry in `.claude/launch.json` starts the dev server that way.

## Pages & design system (Module 05)

- Tokens (Master Plan §8) are CSS variables in `src/app/globals.css` mapped through Tailwind v4 `@theme`: surfaces `--surface-0..3`, text `--text-1..3`, brand indigo→cyan, difficulty/status colours, per-theme chart marks. Dark is the default; `next-themes` toggles `.light`/`.dark`. `/dev/tokens` (admin) shows the live WCAG contrast matrix.
- App routes live in `src/app/(app)` behind `AppShell` (nav rail, top bar, ⌘K palette, page transitions); marketing routes in `src/app/(marketing)`; the workspace keeps its own chrome.
- Pages fetch only through `/api/*` (`src/lib/app/api.ts` + `useQuery`), never Firestore from the browser. New in this module: `GET /api/problems/catalog`, `/api/problems/recommended`, `/api/me/problem-status`, `/api/me/continue`, `DELETE /api/me`, `PATCH /api/projects/:id`, `GET /api/interview`, `GET /api/subscription/invoices`, `GET /api/admin/problems/flagged`, `PATCH /api/admin/problems/:id`, `POST /api/contact`, `GET /api/public/stats`, and `?username=` public variants of `/api/activity`, `/api/me/skills`, `/api/me/achievements`.
- `MAINTENANCE=1` renders the maintenance page; `NEXT_PUBLIC_GA_ID` overrides the GA4 property (loaded only in production).
